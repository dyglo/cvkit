version: '3.8'

services:
  cvkit:
    build: .
    ports:
      - "8080:8080"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - CVKIT_GEMINI_KEY=${CVKIT_GEMINI_KEY}
      - DATABASE_URL=${DATABASE_URL}
    volumes:
      - ./data:/app/data

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: cvkit
      POSTGRES_USER: cvkit
      POSTGRES_PASSWORD: cvkit_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
